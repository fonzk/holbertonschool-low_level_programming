#include <stdio.h>

/**
 * main - use for
 * Description: use putchar to print the alphabet except q and e
 * Return: 0
 */
int main(void)
{
int a;
for (a = 0 ; a <= 9 ; a++)
putchar(a);
putchar('\n');
return (0);
}
