#include <stdio.h>

/**
 * main - use for
 * Description: use putchar to print the alphabet
 * Return: 0
 */
int main(void)
{
int a;
for (a = 'z'; a >= 'a' ; a--)
putchar(a);
putchar('\n');
return (0);
}
