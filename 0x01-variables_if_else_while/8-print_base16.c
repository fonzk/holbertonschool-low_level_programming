#include <stdio.h>

/**
 * main - use for
 * Description: use putchar to print the alphabet
 * Return: 0
 */
int main(void)
{
int a;
for (a = '0'; a <= '9' ; a++)
putchar(a);
for (a = 'a'; a < 'g' ; a++)
putchar(a);
putchar('\n');
return (0);
}
